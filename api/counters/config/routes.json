{
  "routes": [
    {
      "method": "GET",
      "path": "/counters",
      "handler": "counters.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/counters/count",
      "handler": "counters.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/counters/:id",
      "handler": "counters.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/counters",
      "handler": "counters.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/counters/:id",
      "handler": "counters.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/counters/:id",
      "handler": "counters.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
